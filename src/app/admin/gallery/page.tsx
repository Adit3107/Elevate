import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ManageGalleryPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Gallery</CardTitle>
        <CardDescription>Upload, organize, and delete photos from the event gallery.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Gallery management interface will be built here.</p>
      </CardContent>
    </Card>
  );
}
