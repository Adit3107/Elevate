import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ManageAnnouncementsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Announcements</CardTitle>
        <CardDescription>Create and publish announcements for all users to see.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Announcements management interface will be built here.</p>
      </CardContent>
    </Card>
  );
}
