import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ManageResultsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Results</CardTitle>
        <CardDescription>Input and update match results as they happen.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Results management interface will be built here.</p>
      </CardContent>
    </Card>
  );
}
