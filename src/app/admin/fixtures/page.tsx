import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ManageFixturesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Fixtures</CardTitle>
        <CardDescription>Create, edit, and publish tournament fixtures.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Fixture management interface will be built here.</p>
      </CardContent>
    </Card>
  );
}
